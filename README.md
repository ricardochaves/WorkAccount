# Playground With Solidity and Truffle

Getting started with the Ethereum smart contract

Clone the repo, `docker-compose up`, attach `dev` container and run:

```bash
npm install
truffle migration
truffle test
```

## Dev

`.devcontainer` used for a VSCode remote workspace

